# CRI-O rootless cgroup configuration
[crio.runtime]
# Use cgroupfs instead of systemd for rootless operation
# This avoids the need for systemd user session D-Bus
cgroup_manager = "cgroupfs"
conmon_cgroup = "pod"
